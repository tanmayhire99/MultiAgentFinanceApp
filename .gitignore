# Secrets
.env

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
.venv*/
LibreChat/

# OS / IDE
.DS_Store
.vscode/

# Build outputs
FinanceApp.tar.gz

# Runtime response-cache (demo safety net, regenerated per deployment)
data/response_cache/*.json
/test.py

# Persistent per-user memory store (SQLite; per-deployment user state)
data/finai_memory.db
data/finai_memory.db-*

# Event-driven portfolio alerts store (SQLite; per-deployment user state)
data/finai_alerts.db
data/finai_alerts.db-*

# Agent / editor tool config
.claude/

# Runtime session artifacts (regenerated)
session_history/

# Embedded eval repo — intentionally kept out of this repo's index
evals/finlm_eval/

# Personal docs (not project code)
docs/resume.tex
