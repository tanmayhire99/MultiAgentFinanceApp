"""MCP worker agents.

Each module in this package is a standalone MCP server that exposes a small,
domain-specific toolset. Persona agents consume these tools via the
``langchain-mcp-adapters`` MultiServerMCPClient during their ReAct loop.
"""
